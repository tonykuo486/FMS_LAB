# 05 · 源碼剖析 (As-Is, 白箱)

> **目的**: 若接手黑箱系統, 這篇是白箱化的關鍵。逐檔剖析核心引擎邏輯。
> **可跳過**: 若非重構既有系統 (0 → 1 新專案) 不用寫。
> **撰寫日期**: YYYY-MM-DD

---

## 1. 端到端執行鏈

```
[畫出 main() 到最深呼叫的完整鏈, 每個函式標檔案+行號]
```

## 2. 核心模組逐檔剖析

### 2.1 `[file_1.go]`
- **職責**: [一句話]
- **關鍵函式**:
  - `[func_a]` (line xxx): [做什麼]
  - `[func_b]` (line yyy): [做什麼]
- **踩坑點**: [讀源碼時發現的非直覺行為]

### 2.2 `[file_2.go]`
- ...

## 3. DSL / 配置檔解析 (若有)

**配置範例**:
```yaml
[貼一個實際配置檔]
```

**解析流程**: [如何被讀進來 → 轉成什麼 → 用在哪]

## 4. 連線 / 依賴模型

**外部依賴**:
- [Oracle / MySQL / Redis / 第三方 API]

**連線管理**: [pool 大小 / timeout / retry 策略]

## 5. 源碼揭露的新事實

**與原本文件 / 傳說不符的地方**:
- ❗ [事實 1] — 文件說 X, 源碼實作 Y
- ❗ [事實 2] — 系統其實有這個 hidden feature, 但沒人知道
