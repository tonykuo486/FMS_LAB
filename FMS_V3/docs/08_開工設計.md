# 08 · 開工級設計 (Open)

> **目的**: 已可以照這篇開 IDE 寫 code 的設計密度。DDL / Queue / Worker / 告警 / 檢核清單全在。
> **撰寫日期**: YYYY-MM-DD

---

## 1. DDL (若新增 DB 物件)

```sql
-- Table: [name]
CREATE TABLE [name] (
    id ...,
    ...
);

-- Index
CREATE INDEX ...;

-- Trigger (若有)
CREATE TRIGGER ...;
```

**六鐵律 (若用 trigger, 抄 SPD ADR-8)**:
1. Trigger 只寫 outbox, 不做業務邏輯
2. 失敗吞錯 (log 到 aux 表, 不能擋主交易)
3. 冪等: 同一 key 重複觸發不炸
4. ...

## 2. Queue 設計

- **選型**: [Bull / RabbitMQ / SQS / ...]
- **Topic / Queue 命名**: `[namespace]-[event-type]`
- **重試策略**: [n 次 → DLQ]
- **冪等 key**: [如何算]

## 3. Worker 設計

### Ingest Worker
- **職責**: 從 queue 拿 event → 反查 → 冪等寫入
- **並行度**: [N workers]
- **timeout**: [X 秒 / event]

### 對帳 Worker
- **職責**: 每日對帳 shadow vs 正式
- **執行時間**: 每天 [X 點]
- **輸出**: 差異報告 → 告警 or 手動處理

## 4. 告警設計

| 告警 | 觸發條件 | 通知對象 | 嚴重度 |
|---|---|---|---|
| Queue backlog | > N | [人 / channel] | P1 |
| Worker error rate | > X% | | P0 |
| 對帳差異 > 0 | 當日 | | P1 |

## 5. 開工檢核清單

**Kickoff 前必須完成**:
- [ ] DDL 由 DBA 審過
- [ ] Trigger 六鐵律逐條打勾
- [ ] Queue 建好 + monitoring 通
- [ ] Worker 骨架跑通 (hello world event)
- [ ] 告警 channel 建好
- [ ] G0 Kickoff 簽認單 sponsor 簽字 (見 PLAN_TOGAF/07_G0_Kickoff)

## 6. 冪等測試 (必寫)

```
測試: 同一 event 送 3 次 → 只寫入 1 筆
測試: worker 中途 crash → 重啟後從斷點繼續
測試: DB 唯一約束衝突 → 不重試, 直接 skip
```

## 7. Rollback 演練

- **Rollback 觸發條件**: [列出]
- **Rollback SOP**: [step-by-step]
- **Rollback 驗證**: [如何確認 rollback 成功]
